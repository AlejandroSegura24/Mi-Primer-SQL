CREATE TABLE clients (
    name varchar(100),
    email varchar(60),
    phone_number varchar(15)
);

CREATE TABLE products (
    name VARCHAR(20),
    sku VARCHAR(15),
    slug VARCHAR(20),
    description TEXT,
    price FLOAT
);